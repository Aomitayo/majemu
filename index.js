module.exports = () => {
  const majemu = {
    withOpenApi () {
      return majemu
    },
    withExamples (examples) {
      return majemu
    },
    provideWith (providableKey, provider) {
      return majemu
    },
    test (systemUnderTest, done) {
    }
  }

  return majemu
}
